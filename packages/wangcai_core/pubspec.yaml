name: wangcai_core
description: Pure Dart ledger + cloud sync (WebDAV/S3) for WangCai app and CLI.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  crypto: ^3.0.6
  http: ^1.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
